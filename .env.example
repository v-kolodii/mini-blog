# Application environment
APP_ENV=dev
APP_DEBUG=true

# Database (used both by app and docker-compose)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=mini_blog
DB_USER=mini_blog
DB_PASSWORD=secret
DB_ROOT_PASSWORD=root

# Internal app URL
APP_URL=http://localhost:8080
